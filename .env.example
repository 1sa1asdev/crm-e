# Copy to .env.local and fill in your values. .env.local is gitignored.
# These values are inlined into the built JS bundle at build time —
# they are not runtime secrets, only build-time secrets.

VITE_AZURE_CLIENT_ID=
VITE_GMAIL_CLIENT_ID=
VITE_GMAIL_CLIENT_SECRET=
